# automation-reporting-pipeline
This project collects various input files, validates the structure and cleans the formatting of the data received, merges the clean data and generates a final report while logging all the steps. The idea is that this pipeline runs once per day accelerating otherwise manual labor while handling errors and  being transparent about the process. 
